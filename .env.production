# Same-origin: nginx serves this build and proxies /api/ to uvicorn, so the API
# needs no host. deploy.sh and update.sh inject VITE_API_URL="/api" at build
# time anyway — this file keeps a local `npm run build` producing the same thing.
VITE_API_URL=/api
